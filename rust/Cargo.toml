[package]
name = "spanvalue"
version = "0.1.0-alpha.0"
edition = "2021"
description = "Format Cloud Spanner types and column values"
license = "MIT"

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
